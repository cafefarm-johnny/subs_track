# 코드 리뷰 룰 및 기준

이 문서는 AI가 코드 리뷰를 진행할 때 사용하는 기준과 룰을 정의합니다.

## 📋 리뷰 원칙

### 1. 건설적이고 존중하는 피드백
- 코드에 대한 비판이지 사람에 대한 비판이 아님
- 문제점 지적과 함께 개선 방안 제시
- 좋은 코드에 대해서는 칭찬

### 2. 맥락을 고려한 리뷰
- 프로젝트의 아키텍처와 패턴 이해
- 기존 코드 스타일과의 일관성 확인
- 변경사항의 목적과 배경 파악

### 3. 우선순위 기반 리뷰
- **높은 우선순위**: 보안, 버그, 크래시 가능성
- **중간 우선순위**: 코드 품질, 아키텍처, 성능
- **낮은 우선순위**: 스타일, 네이밍 (자동 포맷팅으로 해결 가능)

## 🏗 아키텍처 기준

### MVVM 패턴 준수

#### Model (lib/models/)
- **책임**: 데이터 구조와 비즈니스 로직 없는 순수 데이터 모델
- **체크 포인트**:
  - [ ] 비즈니스 로직이 Model에 포함되지 않았는가?
  - [ ] 불변성(immutability)이 보장되는가? (`final` 필드 사용)
  - [ ] 필요한 getter만 제공하는가?
  - [ ] 데이터 검증 로직이 적절한가?

#### ViewModel (lib/viewmodels/)
- **책임**: 비즈니스 로직, 상태 관리, 데이터 변환
- **체크 포인트**:
  - [ ] UI 로직이 ViewModel에 포함되지 않았는가?
  - [ ] StateNotifier를 올바르게 사용하는가?
  - [ ] 상태 변경이 불변성을 유지하는가? (`state = [...state, newItem]`)
  - [ ] 비동기 작업이 적절히 처리되는가?
  - [ ] 에러 상태가 관리되는가?

#### View (lib/views/)
- **책임**: UI 렌더링, 사용자 입력 처리
- **체크 포인트**:
  - [ ] 비즈니스 로직이 View에 포함되지 않았는가?
  - [ ] Riverpod의 `watch`와 `read`를 올바르게 사용하는가?
  - [ ] 위젯이 적절히 분리되어 있는가?
  - [ ] 불필요한 리빌드가 발생하지 않는가?

### Riverpod 사용 규칙

#### Provider 정의
- [ ] Provider가 적절한 위치에 정의되어 있는가?
- [ ] Provider 이름이 명확한가?
- [ ] StateNotifierProvider를 올바르게 사용하는가?

#### 상태 읽기/구독
- [ ] `ref.watch()`: UI가 반응해야 하는 상태
- [ ] `ref.read()`: 이벤트 핸들러나 일회성 작업
- [ ] `ref.listen()`: 상태 변경 시 사이드 이펙트 필요 시

#### 상태 변경
- [ ] 불변성을 유지하며 상태를 변경하는가?
- [ ] 상태 변경이 필요한 경우에만 수행하는가?

## 💻 코드 품질 기준

### 가독성
- [ ] 코드가 읽기 쉽고 이해하기 쉬운가?
- [ ] 복잡한 로직에 주석이 있는가?
- [ ] 매직 넘버/문자열이 상수로 정의되어 있는가?
- [ ] 함수/메서드가 단일 책임 원칙을 따르는가?

### 네이밍 컨벤션
- [ ] 클래스명: PascalCase (예: `SubscriptionModel`)
- [ ] 변수/메서드명: camelCase (예: `fetchSubscriptions`)
- [ ] 상수: lowerCamelCase 또는 UPPER_SNAKE_CASE
- [ ] private 멤버: `_`로 시작 (예: `_createTitle`)
- [ ] 이름이 의도를 명확히 표현하는가?

### 함수/메서드
- [ ] 함수가 단일 책임을 가지는가?
- [ ] 함수 길이가 적절한가? (일반적으로 20-30줄 이하)
- [ ] 매개변수가 너무 많지 않은가? (5개 이하 권장)
- [ ] 반환 타입이 명확한가?

### 중복 코드
- [ ] 중복된 코드가 없는가?
- [ ] 공통 로직이 함수/유틸리티로 추출되었는가?
- [ ] 비슷한 패턴이 반복되는가?

### 복잡도
- [ ] 순환 복잡도가 낮은가?
- [ ] 중첩된 조건문이 적절한가? (3단계 이상 지양)
- [ ] 조건문이 명확한가?

## 🎨 Flutter/Dart 특화 기준

### 위젯 최적화

#### const 생성자
- [ ] 가능한 모든 위젯에 `const`를 사용하는가?
- [ ] `const`를 사용할 수 있는데 사용하지 않은 경우가 있는가?

#### 위젯 분리
- [ ] 큰 위젯이 작은 위젯으로 분리되어 있는가?
- [ ] 재사용 가능한 위젯이 추출되었는가?
- [ ] private 메서드로 위젯을 분리하는가? (예: `_createTitle`)

#### 리빌드 최적화
- [ ] 불필요한 리빌드가 발생하지 않는가?
- [ ] `ConsumerWidget` vs `ConsumerStatefulWidget` 선택이 적절한가?
- [ ] `ref.watch()` 사용이 최소화되어 있는가?
- [ ] 리스트 렌더링에 `ListView.builder`를 사용하는가?

### 메모리 관리
- [ ] `TextEditingController` 등이 `dispose()`되는가?
- [ ] 스트림 구독이 취소되는가?
- [ ] 타이머가 취소되는가?
- [ ] 메모리 누수 가능성이 없는가?

### 비동기 처리
- [ ] `async/await`를 올바르게 사용하는가?
- [ ] `Future` 에러 처리가 적절한가? (`try-catch` 또는 `.catchError`)
- [ ] Fire-and-Forget 패턴 사용이 적절한가?
- [ ] 비동기 작업 취소가 가능한가? (필요한 경우)

### null 안전성
- [ ] null 안전성을 고려한 코드인가?
- [ ] `?`, `!`, `??` 연산자 사용이 적절한가?
- [ ] null 체크가 충분한가?

## ⚡ 성능 기준

### 리스트 렌더링
- [ ] `ListView.builder`를 사용하는가? (고정된 작은 리스트 제외)
- [ ] `itemExtent`를 지정하는가? (가능한 경우)
- [ ] 불필요한 위젯 생성이 없는가?

### 계산 최적화
- [ ] 반복 계산이 캐싱되는가?
- [ ] 불필요한 연산이 없는가?
- [ ] 비용이 큰 작업이 메인 스레드를 블로킹하지 않는가?

### 이미지 최적화
- [ ] 이미지 크기가 적절한가?
- [ ] 이미지 캐싱이 사용되는가?

## 🔒 보안 기준

### 입력값 검증
- [ ] 사용자 입력이 검증되는가?
- [ ] SQL Injection, XSS 등 취약점이 없는가?
- [ ] 파일 업로드 시 검증이 있는가?

### 민감한 정보
- [ ] API 키, 비밀번호 등이 하드코딩되지 않았는가?
- [ ] 로그에 민감한 정보가 노출되지 않는가?
- [ ] 에러 메시지가 과도한 정보를 노출하지 않는가?

### 권한 처리
- [ ] 필요한 권한만 요청하는가?
- [ ] 권한 거부 시 적절히 처리되는가?

## 🧪 테스트 기준

### 테스트 가능성
- [ ] 코드가 테스트하기 쉬운 구조인가?
- [ ] 의존성이 주입 가능한가?
- [ ] 순수 함수로 분리할 수 있는가?

### 테스트 작성
- [ ] 새로운 기능에 대한 테스트가 있는가?
- [ ] 엣지 케이스에 대한 테스트가 있는가?
- [ ] 테스트가 실제 동작을 검증하는가?

## 🎯 에러 처리 기준

### 에러 처리
- [ ] 예외 상황이 적절히 처리되는가?
- [ ] 사용자에게 적절한 에러 메시지가 표시되는가?
- [ ] 에러 로깅이 적절한가?
- [ ] 에러 복구 전략이 있는가?

### 예외 처리
- [ ] `try-catch` 사용이 적절한가?
- [ ] 구체적인 예외 타입을 잡는가?
- [ ] 예외를 무시하지 않는가?

## 📁 프로젝트 구조 기준

### 파일 구조
- [ ] 파일이 올바른 디렉토리에 위치하는가?
  - `lib/models/`: 데이터 모델
  - `lib/viewmodels/`: 상태 관리 및 비즈니스 로직
  - `lib/views/`: UI 화면
  - `lib/core/`: 핵심 기능 (utils, constants, extensions, theme)
- [ ] 파일명이 명확한가?
- [ ] 한 파일에 하나의 주요 클래스만 있는가?

### import 정리
- [ ] 사용하지 않는 import가 없는가?
- [ ] import 순서가 일관적인가? (dart → package → local)
- [ ] import가 명확한가?

## 🎨 스타일 기준

### 포맷팅
- [ ] `flutter format`을 실행했는가?
- [ ] 들여쓰기가 일관적인가? (2 spaces)
- [ ] 줄 길이가 적절한가? (80-100자)

### 주석
- [ ] 복잡한 로직에 주석이 있는가?
- [ ] 주석이 코드와 일치하는가?
- [ ] TODO/FIXME 주석이 적절한가?
- [ ] 문서 주석(`///`)이 필요한 public API에 있는가?

## 🔍 프로젝트 특화 기준

### Flutter Lints 준수
- [ ] `flutter analyze`를 통과하는가?
- [ ] `analysis_options.yaml`의 규칙을 준수하는가?
- [ ] custom_lint, riverpod_lint 규칙을 준수하는가?

### 다국어 지원
- [ ] 하드코딩된 문자열이 없는가?
- [ ] 다국어 지원이 필요한 경우 `intl`을 사용하는가?

### 다크 모드
- [ ] 다크 모드에서도 올바르게 표시되는가?
- [ ] 색상이 `Theme.of(context)`를 사용하는가?

## 📊 리뷰 우선순위

### 높은 우선순위 (즉시 수정 필요)
1. **보안 취약점**: SQL Injection, XSS, 민감한 정보 노출
2. **크래시 가능성**: null 포인터, 배열 인덱스 오류
3. **데이터 손실**: 저장/로드 로직 오류
4. **메모리 누수**: dispose 누락, 스트림 구독 미취소

### 중간 우선순위 (개선 권장)
1. **코드 품질**: 가독성, 중복 코드, 복잡도
2. **아키텍처**: MVVM 패턴 준수, 의존성 관리
3. **성능**: 불필요한 리빌드, 메모리 사용
4. **에러 처리**: 예외 처리, 사용자 피드백

### 낮은 우선순위 (선택적 개선)
1. **스타일**: 포맷팅, 네이밍 (자동 포맷팅으로 해결 가능)
2. **주석**: 문서화 (코드가 명확하면 불필요할 수 있음)

## 💬 리뷰 코멘트 작성 가이드

### 좋은 리뷰 코멘트 예시

#### 문제점 지적 + 해결 방안
```
이 부분에서 Future.microtask를 사용한 이유가 무엇인가요? 
Fire-and-Forget 패턴을 의도한 것이라면 주석을 추가하는 것이 좋을 것 같습니다.

또는 다음과 같이 명시적으로 처리할 수도 있습니다:
unawaited(ref.read(subscriptionProvider.notifier).fetchSubscriptions());
```

#### 개선 제안
```
이 메서드는 여러 책임을 가지고 있습니다. 
데이터 검증과 상태 업데이트를 분리하는 것이 좋을 것 같습니다.
```

#### 긍정적인 피드백
```
좋은 접근입니다! const 생성자를 사용하여 불필요한 리빌드를 방지하고 있네요.
```

### 나쁜 리뷰 코멘트 예시

#### 모호한 지적
```
이건 잘못됐어요
```

#### 해결 방안 없이 문제만 지적
```
이 코드는 성능이 좋지 않습니다.
```

## 🎯 리뷰 체크리스트

코드 리뷰 시 다음을 확인합니다:

### 필수 체크
- [ ] 보안 취약점이 없는가?
- [ ] 크래시 가능성이 없는가?
- [ ] 메모리 누수가 없는가?
- [ ] MVVM 패턴을 올바르게 따르는가?
- [ ] Riverpod 사용이 적절한가?

### 권장 체크
- [ ] 코드가 읽기 쉬운가?
- [ ] 중복 코드가 없는가?
- [ ] 테스트가 있는가?
- [ ] 에러 처리가 적절한가?
- [ ] 성능 최적화가 되어 있는가?

### 선택적 체크
- [ ] 주석이 적절한가?
- [ ] 네이밍이 명확한가?
- [ ] 포맷팅이 일관적인가?

---

**이 룰을 기준으로 코드 리뷰를 진행합니다. 🚀**

